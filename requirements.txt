# Shopping Shorts Maker - Requirements (cleaned)

# UI & HTTP
PyQt6>=6.6.0
requests>=2.32.5
psutil>=5.9.0

# OCR & Vision
opencv-python>=4.8.0
pytesseract>=0.3.13
numpy>=1.24.0
pillow>=10.0.0
scikit-image>=0.22.0
scipy>=1.11.0
rapidocr-onnxruntime>=1.3.0; python_version < "3.13"
onnxruntime>=1.15.0; python_version < "3.13"

# Whisper / Audio
faster-whisper>=1.2.0
ctranslate2>=4.7.0
av>=11.0
pydub>=0.25.1
audioop-lts>=0.2.0; python_version >= "3.13"
more-itertools>=9.0.0
filelock>=3.16,<4

# Video
moviepy>=1.0.3,<2.0.0
imageio-ffmpeg>=0.4.9

# TTS
edge-tts>=6.1.0

# Downloaders
yt-dlp>=2023.11.0

# Progress
tqdm>=4.66.0

# File type
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'

# AI APIs
google-genai>=2.4.0,<2.9.0
google-cloud-aiplatform>=1.153.1
google-auth>=2.53.0
google-api-python-client>=2.196.0
google-auth-oauthlib>=1.4.0
google-auth-httplib2>=0.4.0
# Explicit OAuth transport dependencies: release builds must not rely on a
# developer machine's incidental/transitive packages.
httplib2>=0.31.0,<1
requests-oauthlib>=2.0.0,<3
oauthlib>=3.3.1,<4
anthropic>=0.18.0

# JWT / Security
PyJWT==2.13.0
cryptography>=44.0.1

# Backend (FastAPI stack) - keep aligned with backend/requirements.txt
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
psycopg[binary]==3.3.4
pymysql==1.1.2
pydantic==2.13.4
pydantic-settings==2.14.2
passlib[bcrypt]==1.7.4
bcrypt>=4.2.1
slowapi==0.1.9
python-multipart>=0.0.22
alembic==1.14.0

# Windows
pywin32>=305; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'
platformdirs>=3.10.0
colorama>=0.4.6
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Scraping & Automation
selenium>=4.15.0
webdriver-manager>=4.0.0
beautifulsoup4>=4.12.0
# zendriver drives the full-automation sourcing pipeline (core/sourcing/pipeline.py).
# Previously only installed by the _install_*.py helpers, so a plain
# `pip install -r requirements.txt` left the pipeline unable to launch a browser.
# Requires Python >= 3.10.
zendriver>=0.15.0
