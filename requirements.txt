# Shopping Shorts Maker - Requirements
# Python 3.14 호환 버전

# UI 및 네트워크
PyQt5>=5.15.0  # GUI 프레임워크
requests>=2.31.0  # HTTP 라이브러리

# 기본 패키지
psutil>=5.9.0  # 시스템 정보 수집 및 모니터링

# OCR 및 이미지 처리
opencv-python>=4.8.0
pytesseract>=0.3.13
numpy>=1.24.0
pillow>=10.0.0

# RapidOCR (Python 3.12 이하만 지원)
rapidocr-onnxruntime>=1.3.0; python_version < "3.13"
onnxruntime>=1.15.0; python_version < "3.13"

# Whisper 음성 인식 (Python 3.12 이하만 지원)
# Python 3.13+ 에서는 글자 수 비례 폴백 사용
faster-whisper>=1.0.0; python_version < "3.13"
ctranslate2>=4.0.0; python_version < "3.13"

# HuggingFace (Python 3.14 호환)
huggingface-hub>=0.20.0
tokenizers>=0.15.0; python_version < "3.13"

# 오디오 처리
pydub>=0.25.1
more-itertools>=9.0.0

# 비디오 및 이미지 처리
moviepy>=1.0.3
imageio-ffmpeg>=0.4.9  # moviepy FFmpeg 바이너리

# AI API 클라이언트
google-genai>=1.0.0  # Gemini API (새 SDK)
anthropic>=0.18.0  # Claude API

# Windows 시스템 정보 (Windows 전용)
pywin32>=305; sys_platform == 'win32'
wmi>=1.5.1; sys_platform == 'win32'

# 플랫폼 호환성
platformdirs>=3.10.0

# 로깅 및 디버깅
colorama>=0.4.6

# 보안 및 암호화
cryptography>=41.0.0  # Fernet(AES) 암호화

# 설치 방법:
# pip install -r requirements.txt
