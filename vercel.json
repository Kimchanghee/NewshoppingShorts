{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd backend && python -m alembic upgrade head",
  "functions": {
    "api/index.py": {
      "includeFiles": "backend/**",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
